// EuclideanPbinds quickstart
// E(3,8), the tresillo, as data and as a playable pattern.
(
EuclideanRhythm.visualise(3, 8).postln;
EuclideanRhythm.onsetIndices(3, 8).postln;
)

// Uncomment for sound:
// EuclideanPbind(3, 8, (instrument: \default, dur: 0.25, amp: 0.2)).play;
